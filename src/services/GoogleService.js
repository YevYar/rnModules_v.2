/**
 * This module contains service that works with react-native-google-signin.
 *
 * @format
 */

import { GoogleSignin } from 'react-native-google-signin';

// export default appName => AppInstalledChecker.isAppInstalled(appName);
