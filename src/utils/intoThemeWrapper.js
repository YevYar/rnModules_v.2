/**
 * This module contains wrapper (HOC) that provides theme props to the components.
 *
 * @format
 */

import { withTheme } from 'react-native-material-ui';

export default component => withTheme(component);
